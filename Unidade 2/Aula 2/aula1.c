#include <stdio.h>
#include <stdlib.h>
#include <locale.h>

int main()
{
    setlocale(LC_ALL, "pt_BR.UTF-8");

    // int num, i;

    // printf("Digite um número para ver sua tabuada!\n");
    // scanf("%d", &num);

    // for (i = 1; i <= 10; i++)
    // {
    //     printf("%d x %d = %d\n", num, i, num * i);
    // }

    

    return 0;
}